# JWT Secret - MUSS geaendert werden! Generieren mit: openssl rand -hex 64
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET

# MongoDB Verbindung (bei Docker Compose so belassen)
MONGODB_URI=mongodb://mongo:27017/haushaltsplaner

# Erlaubte CORS-Origins (* = alle, oder spezifische Domain)
FRONTEND_URL=*

# Port fuer das Backend
PORT=3000

# Environment (production oder development)
NODE_ENV=production

# Firebase Push-Benachrichtigungen (optional, ohne diese Variable deaktiviert)
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'
